span
    codemirror(v-if="type=='code'", :value="getValue()", :options="{readOnly: readonly, mode: contentType}", :class="className", :style="(css ? css : {maxWidth: '333px'})", @on-change="changeValue", :tabindex="index")
    Input(v-if="type=='text'", :placeholder="placeholder", :value="getValue()", :disabled="disabled", :readonly='readonly', :style="(css ? css : {maxWidth: '333px'})", @on-change="changeValue", :class="className", :title="title", :index="index", @on-focus="focus")
        span(v-if="isString(prepend)", slot="prepend") {{prepend}}
        Select(v-else-if="isArray(prepend)", transfer, transfer-class-name="cc-prepend", :value="getDefaultPrepend()", @on-select="changePrependValue", slot="prepend", :style="{width: getMaxWidth(prepend)}")
            Option(v-for="(item, index) in prepend", :value="(typeof(item)=='object' && item.value ? item.value : item)", :key="(typeof(item)=='object' && item.value ? item.value : item)") {{(typeof(item)=='object' && item.text ? item.text : item)}}
        span(v-if="isString(append)", slot="append")
        Select(v-else-if="isArray(append)", transfer, transfer-class-name="cc-append", :value="getDefaultAppend()", @on-select="changeAppendValue", slot="append", :style="{width: getMaxWidth(prepend)}")
            Option(v-for="(item, index) in append", :value="(typeof(item)=='object' && item.value ? item.value : item)", :key="(typeof(item)=='object' && item.value ? item.value : item)") {{(typeof(item)=='object' && item.text ? item.text : item)}}
    DatePicker(v-else-if="type=='date'", transfer, confirm, :type="'date'", :placeholder="placeholder", :value="getValue()", :disabled="disabled", :prepend="prepend", :append="append", :readonly='readonly', :style="(css ? css : {})", @on-change="changeValue", :class="className", :title="title", :index="index", @on-focus="focus")
    DatePicker(v-else-if="type=='daterange'", transfer, confirm, :type="'daterange'", :placeholder="placeholder", :value="getValue()", :disabled="disabled", :prepend="prepend", :append="append", :readonly='readonly', :style="(css ? css : {})", @on-change="changeValue", :class="className", :title="title", :index="index", @on-focus="focus")
    TimePicker(v-else-if="type=='time'", transfer, confirm, :type="'time'", :placeholder="placeholder", :value="getValue()", :disabled="disabled", :prepend="prepend", :append="append", :readonly='readonly', :style="(css ? css : {})", @on-change="changeValue", :class="className", :title="title", :index="index", @on-focus="focus")
    TimePicker(v-else-if="type=='timerange'", transfer, confirm, :type="'timerange'", :placeholder="placeholder", :value="getValue()", :disabled="disabled", :prepend="prepend", :append="append", :readonly='readonly', :style="(css ? css : {})", @on-change="changeValue", :class="className", :title="title", :index="index", @on-focus="focus")
    Input(v-else-if="type=='password'", type="password", password, :placeholder="placeholder", :value="getValue()", :disabled="disabled", :prepend="prepend", :append="append", :readonly='readonly', :style="(css ? css : {maxWidth: '333px'})", @on-change="changeValue", :class="className", :title="title", :index="index", @on-focus="focus")
    input(v-else-if="type=='hidden'", type="hidden", :value="getValue()", @change="changeValue", :index="index", :attribute="attribute", :ref="attribute")
    Input(v-else-if="type=='textarea'", type="textarea", :placeholder="placeholder", :rows="rows", :value="getValue()", :autosize="true", :disabled="disabled", :readonly='readonly', :style="(css ? css : {})", @on-change="changeValue", :class="className", :title="title", :index="index", @on-focus="focus")
    label(v-else-if="type=='color'")
        input(type="color", :value="getColorHex(value)", :attribute="attribute", :ref="attribute", onchange="document.getElementById(this.getAttribute('attribute') + '_color_hidden_value').value=this.value; document.getElementById(this.getAttribute('attribute') + '_color_hidden_value').click();",
            :disabled="disabled", :readonly='readonly', :style="(css ? css : {})", :class="className", :title="title", :index="index")
        input(:id="this.attribute + '_color_hidden_value'", value="", type="hidden", @click="changeValue")

    label(v-else-if="type=='file' && base64===false")
        Button(icon="md-images", @click="openFileDialog", :disabled="disabled", :readonly='readonly', :style="(css ? css : {})", :class="className", :title="title", :index="index") {{(title || '上传')}}
        input(:attribute="attribute", :id="this.attribute + '_file_'", :accept="options.join(',')", style="position:absolute;top:-1000px;left:-1000px", type="file", @change="changeValue(getFile())")

    label(v-else-if="type=='file'")
        Button(icon="md-images", @click="openFileDialog", :disabled="disabled", :readonly='readonly', :style="(css ? css : {})", :class="className", :title="title", :index="index") {{(title || '上传')}}
        input(:attribute="attribute", :id="this.attribute + '_file_'", :accept="options.join(',')", style="position:absolute;top:-1000px;left:-1000px", type="file", onchange="var attrName=this.getAttribute('attribute'); var file=this.files[0]; var reader=new FileReader(); reader.onload=function(e){ var data=e.target.result; document.getElementById(attrName + '_file_hidden_value').value='url('+data+')'; document.getElementById(attrName + '_file_hidden_value').click();};reader.readAsDataURL(file);")
        input(:id="this.attribute + '_file_hidden_value'", value="", type="hidden", @click="changeValue")
    Switch(v-else-if="type=='switch'", size="small", :value="getValue()", :disabled="disabled", :readonly='readonly', :style="(css ? css : {})", @on-change="changeValue", :class="className", :title="title", :index="index", @on-focus="focus")
    InputNumber(v-else-if="type=='number'", :placeholder="placeholder", :value="isNaN(this.value) || this.value==undefined || this.value==null? '' : Number(this.value)", :disabled="disabled", :readonly='this.readonly', :style="(css ? css : {})", @on-change="changeValue", :class="className", :title="title", :max="max" :min="min", :index="index", @on-focus="focus")
    InputNumber(v-else-if="type=='decimal'", :step="0.1", :placeholder="placeholder", :value="isNaN(this.value) || this.value==undefined || this.value==null ? '' : Number(this.value)", :disabled="disabled", :readonly='this.readonly', :style="(css ? css : {})", @on-change="changeValue", :class="className", :title="title", :max="max" :min="min", :index="index", @on-focus="focus")
    Select(v-else-if="type=='select'", transfer, transfer-class-name="cc-select", :placeholder="placeholder", :value="getValue()", :disabled="disabled", :readonly='readonly', :style="(css ? css : {})", @on-select="changeValue", :class="className", :title="title", :index="index", @on-open-change="focus")
        Option(v-for="(item, index) in options", :value="(typeof(item)=='object' && item.value ? item.value : item)", :key="(typeof(item)=='object' && item.value ? item.value : item)") {{(typeof(item)=='object' && item.text ? item.text : item)}}
    Select(v-else-if="type=='search' && onSearch", transfer, transfer-class-name='cc-select', :default-label="defaultLabel", :remote-method="search", :loading="loading", :placeholder="placeholder", filterable, :value="getValue()", :disabled="disabled", :readonly='readonly', :style="(css ? css : {})", @on-select="changeValue", :class="className", :title="title", :index="index", @on-open-change="focus")
        Option(v-for="(item, index) in options", :value="(typeof(item)=='object' && item.value ? item.value : item)", :key="(typeof(item)=='object' && item.value ? item.value : item)") {{(typeof(item)=='object' && item.text ? item.text : item)}}
    Select(v-else-if="type=='search'", transfer, transfer-class-name="cc-select", :placeholder="placeholder", filterable, :value="getValue()", :disabled="disabled", :readonly='readonly', :style="(css ? css : {})", @on-select="changeValue", :class="className", :title="title", :index="index", @on-open-change="focus")
        Option(v-for="(item, index) in options", :value="(typeof(item)=='object' && item.value ? item.value : item)", :key="(typeof(item)=='object' && item.value ? item.value : item)") {{(typeof(item)=='object' && item.text ? item.text : item)}}
    Select(v-else-if="type=='multiple'", transfer, transfer-class-name="cc-select", :placeholder="placeholder", multiple, :value="getValue()", :disabled="disabled", :readonly='readonly', :style="(css ? css : {})", @on-select="changeValue", :class="className", :title="title", :index="index", @on-open-change="focus")
        Option(v-for="(item, index) in options", :value="(typeof(item)=='object' && item.value ? item.value : item)", :key="(typeof(item)=='object' && item.value ? item.value : item)") {{(typeof(item)=='object' && item.text ? item.text : item)}}
    Select(v-else-if="type=='multiple-search' && onSearch", transfer, transfer-class-name="cc-select", :remote-method="search", :default-label="defaultLabel", @on-set-default-options="onSearch", :loading="loading", :placeholder="placeholder", multiple, filterable, :value="getValue()", :disabled="disabled", :readonly='readonly', :style="(css ? css : {})", @on-select="changeValue", :class="className", :title="title", :index="index", @on-open-change="focus")
        Option(v-for="(item, index) in options", :value="(typeof(item)=='object' && item.value ? item.value : item)", :key="(typeof(item)=='object' && item.value ? item.value : item)") {{(typeof(item)=='object' && item.text ? item.text : item)}}
    Select(v-else-if="type=='multiple-search'", transfer, transfer-class-name="cc-select", :placeholder="placeholder", multiple, filterable, :value="getValue()", :disabled="disabled", :readonly='readonly', :style="(css ? css : {})", @on-select="changeValue", :class="className", :title="title", :index="index", @on-open-change="focus")
        Option(v-for="(item, index) in options", :value="(typeof(item)=='object' && item.value ? item.value : item)", :key="(typeof(item)=='object' && item.value ? item.value : item)") {{(typeof(item)=='object' && item.text ? item.text : item)}}
    RadioGroup(v-else-if="type=='radio'", :value="getValue()", vertical, @on-change="changeValue", :class="className", :title="title", :index="index", @on-focus="focus")
        Radio(v-for="(item, index) in options", :style="(css ? css : {float: 'left'})", :label="(typeof(item)=='object' && item.value ? item.value : item)", :key="(typeof(item)=='object' && item.value ? item.value : item)", :disabled="disabled")
            Icon(:type="icon", v-if="icon!==undefined")
            span {{(typeof(item)=='object' && item.text ? item.text : item)}}
    RadioGroup(v-else-if="type=='radio-button'", :value="getValue()", vertical, @on-change="changeValue", :class="className", :title="title", :index="index", type="button", @on-focus="focus")
        Radio(v-for="(item, index) in options", :style="(css ? css : {float: 'left'})", :label="(typeof(item)=='object' && item.value ? item.value : item)", :key="(typeof(item)=='object' && item.value ? item.value : item)", :disabled="disabled") {{(typeof(item)=='object' && item.text ? item.text : item)}}
    CheckboxGroup(v-else-if="type=='checkbox' && options && options.length && options.length>0", :value="getValue()", vertical, @on-change="changeValue", :class="className", :title="title", :index="index", @on-focus="focus")
        Checkbox(v-for="(item, index) in options", :style="(css ? css : {float: 'left'})", :label="(typeof(item)=='object' && item.value ? item.value : item)", :key="(typeof(item)=='object' && item.value ? item.value : item)", :disabled="disabled")
            Icon(type="this.icon", v-if="icon!==undefined")
            span(v-if="typeof(item)=='string'") {{item}}
            span(v-else-if="item.text") {{item.text}}
    Checkbox(v-else-if="type=='checkbox'", :value="getValue()", vertical, @on-change="changeValue", :class="className", :title="title", :index="index", @on-focus="focus") {{title}}
    .ccinput-tag(v-else-if="type=='tag'")
        Tag(v-for="item in getTags()", :color="randomColor()", :key="item", :name="item", closable, @on-close="removeTag", @on-change="onSearch ? onSearch : ()=>{}", :type="className", :class="className", :title="title", :index="index", :style="(css ? css : {})") {{item}}
        Button(size="small", style="border: none;")
            Input(size="small", style="max-width:120px;margin-top: 2px;", v-model="newTagValue", :placeholder="placeholder", @on-enter="addTag")
            Button(icon="ios-add", size="small", @click="addTag", type="dashed")

